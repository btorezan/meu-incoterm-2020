import 'package:flutter/material.dart';

class IncotermsScreen extends StatelessWidget {
  const IncotermsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
